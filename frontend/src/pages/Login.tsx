export default function Login() {
  return (
    <section className="section">
      <h2>Entrar</h2>
      <p style={{ color: 'var(--muted)' }}>Tela de autenticação. Em breve integração com o backend Laravel (JWT/cookies).</p>
    </section>
  )
}
